# 方案

用户偏好：

- 排版引擎：待论文阶段确认
- 竞赛类型：待确认
- 论文语言：默认中文，若竞赛规则另有要求则调整
- 子问题数量：待读取题面后确定
- 项目节奏：三天实战优先
- 模型方向：传统、成熟、可实现模型优先
- 图形风格：低饱和度、高区分度统一色板
- 命名方式：文件名和变量名尽量中文，单位使用英文
- Python 环境：VS Code 当前选中的全局 Python 解释器
- 特别约束：不生成独立 ANALYSE/ANALYSIS 建模报告，代码不写注释，代码路径不得包含用户姓名

workflow:

1. 题面与附件核对、子问题拆解和传统模型选型；分析结论直接进入实现过程，不单独生成 ANALYSE 报告。
2. 按 `2605` 的目录形式完成代码、测试、计算结果和数据图表。
3. 仅在论文确有需要时绘制流程图或模型结构图。
4. 根据比赛类型和排版引擎撰写论文。
5. 检查约束、数值一致性、代码可复现性、图表质量和提交完整性。

预期产物：

- `code/`：各子问题 Python 代码
- `outputs/`：CSV、JSON、TXT 等计算结果
- `figures/`：PDF 与 PNG 图表
- `reports/`：各子问题结果报告，不含独立分析建模报告
- `test/`：各子问题测试代码
- `paper/`：确认排版引擎后生成的论文文件

风险控制：

- 开始计算前核对附件字段、单位、缺失值和约束口径。
- 优先建立可运行基线模型，再做必要改进。
- 每个子问题保留独立输出和测试，避免结果互相覆盖。
- 所有论文数值只引用已生成的结果文件或图表。
