# 项目执行规则

本项目开始任何数学建模、编程、绘图或写作前，必须先读取 `全局说明.md`。

- 目录与成果组织参照 `C:/Users/当前用户/Desktop/2605/` 的形式，但代码中不得写入用户姓名或含用户姓名的绝对路径。
- 不生成 `ANALYSIS_MODELING_REPORT.md`、`ANALYSE` 报告或同类独立分析报告；题意分析和模型设计直接服务于代码、结果报告与论文。
- 优先选择三天竞赛周期内可实现、可解释、可复现的传统模型；除非题目确有必要，不引入复杂创新模型。
- Python 文件名和变量名在清晰可读的前提下尽量使用中文，单位统一使用英文符号。
- Python 代码不写注释，文件读写使用相对路径或运行时定位，不写含用户名的绝对路径。
- 使用 VS Code 当前选中的全局 Python 解释器运行代码，不创建项目虚拟环境。
- 所有图形使用低饱和度、高区分度配色，具体色板见 `全局说明.md`。
